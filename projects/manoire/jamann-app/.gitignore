# Secrets — NE JAMAIS COMMITER
.env
.env.*
*.key
*.pem
secrets/
.anthropic_api_key

# Netlify — état local du site (contient un siteId, pas une clé, mais autant ne pas le pousser)
.netlify/

# Node
node_modules/
npm-debug.log*

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/

# Build outputs locaux (rien à construire, mais au cas où)
dist/
